# Production Profile (Supabase Postgres Database)
# Commented out to allow DB_PLATFORM env var from application.properties to take precedence.
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
